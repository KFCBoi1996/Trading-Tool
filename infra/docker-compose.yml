version: "3.9"
services:
  api:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ../:/app
    command: bash -lc "pip install -e .[dev] && uvicorn trading_tool.api.app:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
